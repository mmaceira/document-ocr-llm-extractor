line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "W", "I", "UP", "B", "RET", "SIM"]

[format]
# Ruff format follows a specific style guide
# The assert statement formatting is not configurable
quote-style = "double"
indent-style = "space"
